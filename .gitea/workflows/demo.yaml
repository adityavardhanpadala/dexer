name: Dexer Build 
run-name: ${{ gitea.actor }}
on: [push]

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y"
      - run: "cargo check"
